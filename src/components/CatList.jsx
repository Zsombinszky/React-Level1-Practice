import React from "react";
// The component receives cats data as props from CatContent

const CatList = ({ cats }) => {
  return (
    <>
      {/* 
      1. Create an unordered list container
      2. Display each cat's information as a list item
         - Each item should show:
           - The cat's name as a heading
           - Three separate pieces of information:
             - Type
             - Color 
             - Age
      */}
    </>
  );
};

export default CatList;
