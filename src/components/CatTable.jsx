import React from "react";
// The cats data is received as props from the CatContent component

const CatTable = ({ cats }) => {
  return (
    <>
      {/* Create a table displaying all cat information
          - Include a header row (thead) with column names
          - In the table body (tbody), display all cat data
          - Each cat's data should be in a separate row (tr)
          - Individual data points should be in cells (td) */}
    </>
  );
};

export default CatTable;
