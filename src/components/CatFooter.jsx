import React from "react";
// Import the cats array from data/cats.js

// Create a function that:
// 1. Returns all cat types
// 2. Capitalizes each type name
// 3. Returns the result in a new array
const getCatTypesAndCapitalize = () => {};

// Store the modified cat types in a variable

const CatFooter = () => {
  return (
    <div className="py-8 grid grid-cols-5 h-[10%] w-full bg-black text-white text-xl">
      {/* Iterate through the cat types array:
          - Create an anchor element (a tag) for each type
          - Set href to empty string (href="")
          - The link text should be the cat type name */}
    </div>
  );
};

export default CatFooter;
