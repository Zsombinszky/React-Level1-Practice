import React from "react";

// Component receives cat data as props
// Design a reusable card component displaying cat information

const CatCard = ({ cat }) => {
  return (
    <div>
      {/* Image section: 
          - Display cat image 
          - Use appropriate alt text */}

      {/* Cat name: Display as prominent heading */}

      {/* Information sections:
          - Age
          - Color 
          - Type
          - Favorite food
      (Each as separate text elements) */}
    </div>
  );
};

export default CatCard;
